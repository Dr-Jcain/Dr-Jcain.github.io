document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.quiz-card').forEach(card=>{const answer=card.dataset.answer;const feedback=card.querySelector('.quiz-feedback');card.querySelectorAll('button[data-choice]').forEach(btn=>btn.addEventListener('click',()=>{card.querySelectorAll('button[data-choice]').forEach(b=>b.classList.remove('correct','wrong'));const ok=btn.dataset.choice===answer;btn.classList.add(ok?'correct':'wrong');feedback.textContent=ok?'Correcto.':'Revise el concepto y vuelva a intentarlo.';}));});});
